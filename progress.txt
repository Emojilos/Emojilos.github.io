# BrowserStrike — Notes for next iteration

## Completed this session
- TASK-017 (Lobby UI: room code, team selection, settings, start game)
  - Created packages/client/src/ui/LobbyScreen.ts:
    - Room code display (large, monospace, orange) + Copy button with clipboard API
    - Two team columns (Team A blue, Team B red) with player lists
    - Join Team A / Join Team B buttons, highlights current team
    - Unassigned players section
    - Admin settings panel: mode (1v1/2v2), map (all 4 maps), rounds to win (5/7/10/13)
    - Non-admin sees settings as read-only (disabled selects)
    - Start Game button: visible only to admin, disabled until teams staffed
    - Leave button returns to menu
    - Status messages: waiting for players, waiting for host
    - HOST badge on admin player, local player highlighted orange
    - update() method syncs from Colyseus state, render() rebuilds UI
    - escapeHtml() for XSS prevention on nicknames
  - Updated packages/server/src/rooms/GameRoom.ts:
    - 'updateSettings' handler: admin-only, validates mode/mapId/roundsToWin
    - Rebuilds CollisionWorld when mapId changes
    - 'startGame' handler: admin-only, validates teams staffed per mode
    - Transitions status to 'weapon_select' on start
  - Updated packages/client/src/App.ts:
    - Added initLobbyScreen() — creates LobbyScreen and wires callbacks
    - onJoinTeam sends 'joinTeam', onUpdateSettings sends 'updateSettings'
    - onStartGame sends 'startGame', onLeave disconnects and returns to menu
    - syncLobbyState() reads Colyseus state and updates LobbyScreen
    - Polls lobby state every 200ms via setInterval while in LOBBY
    - Auto-transitions to PLAYING when server status becomes weapon_select/playing
    - Cleans up interval on leaving LOBBY state
  - Updated packages/client/index.html:
    - Full CSS for lobby panel, team columns, player lists, settings, buttons

- TASK-016 (Main Menu UI: nickname input, create/join room)
  - Created packages/client/src/ui/MenuScreen.ts:
    - Dark theme panel (#1a1a2e bg, #16213e panel, #ff6b00 accents)
    - Nickname input with validation (3-16 chars, a-zA-Z0-9_)
    - "Create Room" button calls NetworkManager.createRoom()
    - "Join" button + room code field (6 chars, auto-capitalize) calls NetworkManager.joinByCode()
    - Error display area for validation/network errors
    - Loading state (disables inputs, shows "Connecting...")
    - Nickname persisted to localStorage
    - Reset method for re-entering menu state
  - Updated packages/client/index.html:
    - Added full CSS for menu panel, inputs, buttons, divider, error display
    - Background changed to #1a1a2e (dark theme)
  - Updated packages/client/src/App.ts:
    - Added initMenuScreen() — creates MenuScreen and wires callbacks
    - onCreateRoom/onJoinRoom callbacks transition to LOBBY on success
    - enterState(MENU) calls menuScreen.reset()

- TASK-020 (Position sync: send inputs to server, display remote players)
  - Created packages/client/src/engine/RemotePlayerManager.ts:
    - Spawns/despawns Three.js capsule meshes for remote players
    - Cylinder body + sphere head, team-colored (blue=A, red=B, grey=unassigned)
    - Updates position + yaw from Colyseus state, auto-recolors on team change
    - Proper dispose: removes from scene, disposes geometry + material
  - Updated packages/client/src/App.ts:
    - Added inputSeq counter, sends InputMessage ('input') to server each frame
    - sendInput(): builds {seq, keys, yaw, pitch, deltaTime} from FPSController state
    - Instantiates RemotePlayerManager on PLAYING enter, disposes on leave
    - setupNetworkListeners(): wires onPlayerAdd/onPlayerRemove/onStateChange
    - syncRemotePlayers(): reads Colyseus state.players, updates capsule positions
    - Only sends input when pointer is locked (no spam when unfocused)

- TASK-015 (Server game loop + move/look message handling)
  - Created packages/server/src/physics/CollisionWorld.ts:
    - Server-side collision world (pure math, no Three.js)
    - Same AABB resolve logic as client (4-iteration correction)
  - Created packages/server/src/physics/mapCollisions.ts:
    - buildWarehouseCollisions() — same collision boxes as client MapBuilder
    - buildMapCollisions(mapId) — factory for per-map collision worlds
  - Updated packages/server/src/rooms/GameRoom.ts:
    - 'input' message handler: validates input, applies applyMovement(), resolves collisions
    - Input validation: rejects NaN, Infinity, negative/excessive deltaTime, wrong types
    - setSimulationInterval at TICK_RATE (20Hz) — placeholder onTick for future round logic
    - CollisionWorld built on room creation based on map settings
  - Updated packages/server/src/schemas/PlayerSchema.ts:
    - Added lastProcessedSeq (uint32) for client-side prediction reconciliation
    - Added server-only velocityY and isGrounded (not replicated, used for physics)

- TASK-014 (Client router and state machine)
  - Created packages/client/src/App.ts:
    - AppState enum: MENU, LOBBY, PLAYING, MATCH_END
    - App class owns SceneManager, FPSController, WeaponModel, NetworkManager
    - setState() transitions between screens, shows/hides div containers in #ui-root
    - Game loop starts on PLAYING enter, stops on leave (disposes 3D resources)
    - Canvas hidden when not in PLAYING state
    - Screens created dynamically if not found in HTML
  - Refactored main.ts from flat script to App instantiation
    - App instance exposed as window.app for console testing
  - Updated index.html:
    - Added screen div containers (menu-screen, lobby-screen, playing-screen, match-end-screen)
    - Canvas hidden by default (display:none)
    - Added .screen CSS class for full-viewport screen layout

- TASK-013 (Room System: create/join by code, nickname validation)
  - Enhanced GameRoom.ts:
    - Added nickname validation: 3-16 chars, latin letters + digits + underscores only
    - Throws ServerError(400) for invalid nicknames
    - Room code exposed as metadata via setMetadata() for client room discovery
    - Added 'joinTeam' message handler (team A/B/unassigned)
    - Better logging with player count
  - Created packages/client/src/network/NetworkManager.ts:
    - createRoom(nickname) — creates room, returns room code
    - joinByCode(roomCode, nickname) — queries available rooms by metadata, joins matching
    - listen(callbacks) — registers state change, player add/remove, error, leave callbacks
    - send(type, data) / onMessage(type, callback) for message passing
    - leave() for disconnection
    - sessionId, connected, currentRoom getters
    - Uses VITE_SERVER_URL env var (defaults to ws://localhost:2567)
  - Created packages/client/src/vite-env.d.ts for Vite type support
  - Fixed server dev script: added --tsconfig flag to tsx for legacy decorator support

## Completed this session (continued)
- TASK-019 (Client-side shooting system: raycast, visual effects, ammo tracking)
  - Created packages/client/src/engine/ShootingSystem.ts:
    - Three.js Raycaster from camera position (eye height) in look direction
    - Weapon spread system: base spread, moving spread, sustained (consecutive shots) spread
    - Spread resets after 300ms of no firing
    - Muzzle flash: PointLight at 0xffaa00, intensity 3, fades after 50ms
    - Tracer lines: BufferGeometry line from origin to hit point, yellow (#ffdd44), fades over 150ms
    - Wall decals: CircleGeometry (0.03 radius), oriented to surface normal, ring buffer of 64 max
    - Optimistic ammo tracking: decrements on fire, blocks firing at 0
    - Sends ShootMessage to server: {seq, timestamp, origin, direction}
    - Hit normal transformed from object-local to world space for correct decal orientation
    - Objects with userData.isEffect are skipped during raycasting (prevents self-intersection)
    - Proper dispose(): removes all lights, tracers, decals; disposes geometries and materials
  - Updated packages/client/src/App.ts:
    - ShootingSystem created in startGameLoop(), disposed in stopGameLoop()
    - setSendCallback wired to network.send('shoot', msg)
    - animate() checks ammo > 0 before allowing weapon.tryFire()
    - On successful fire: computes isMoving from WASD keys, calls shooting.fire()
    - shooting.update(dt) called each frame for tracer fade and muzzle flash

## Completed this session (continued 2)
- TASK-018 (Game HUD: crosshair, HP, ammo, round score, timer)
  - Created packages/client/src/ui/GameHUD.ts:
    - CSS crosshair: green cross (4 lines + center dot), centered, pointer-events: none
    - HP display: bottom center, health icon (+) with value, color-coded (white >60, orange >25, red ≤25)
    - Ammo display: bottom right, "current / magazine" + weapon name, color-coded (red at 0, orange ≤30%)
    - Round score: top center, Team A (blue) : Team B (red) with separator
    - Round timer: below score, minutes:seconds format, color-coded (red ≤10s, orange ≤30s)
    - All elements use text-shadow for readability against 3D scene
    - update(HUDState) method refreshes all elements from provided state
    - dispose() removes container from DOM
  - Updated packages/client/index.html:
    - Full CSS for .game-hud and all sub-elements
    - Crosshair: 20px arms, 2px thickness, #00ff44 green, box-shadow outline
    - Positioned elements: absolute positioning for each HUD area
  - Updated packages/client/src/App.ts:
    - GameHUD created in startGameLoop(), disposed in stopGameLoop()
    - HUD updated every frame in animate() with current game state
    - Helper methods: getLocalPlayerHP(), getLocalWeaponId(), getScoreA(), getScoreB(), getRoundTime()
    - HP read from Colyseus state.players local player, defaults to PLAYER_HP
    - Score/timer read from Colyseus state, defaults to 0 / ROUND_TIME_LIMIT
    - WeaponId currently hardcoded to 'deagle' (will be dynamic after weapon switching)

## Completed this session (continued 3)
- TASK-023 (Ammo & reload system — client + server)
  - Updated packages/client/src/engine/ShootingSystem.ts:
    - Added reload state machine: _isReloading, reloadTimer, reloadTime (from weapon config)
    - startReload(): validates not already reloading and magazine not full, starts timer, notifies server
    - cancelReload(): cancels in-progress reload (for future weapon switch)
    - completeReload(): refills magazine to magazineSize
    - syncAmmo(): accepts server authoritative ammo/isReloading, overrides client state
    - Auto-reload: when ammo hits 0 and not already reloading, startReload() called in update()
    - fire() blocks during reload (returns false if _isReloading)
    - getIsReloading(), getReloadProgress() getters for HUD
    - setReloadCallback() for sending ReloadMessage to server
  - Updated packages/client/src/engine/InputManager.ts:
    - Added reloadPressed flag (edge-triggered, not continuous)
    - R key ('KeyR') sets reloadPressed = true on keydown
    - consumeReload() returns and resets the flag (consumed per frame)
  - Updated packages/server/src/rooms/GameRoom.ts:
    - 'shoot' message handler: validates player alive, not reloading, has ammo, fire rate enforcement via lastShootTime, decrements ammo
    - 'reload' message handler: validates not already reloading, magazine not full, sets isReloading=true, schedules clock.setTimeout for reloadTime ms, completes by setting isReloading=false and refilling ammo
  - Updated packages/server/src/schemas/PlayerSchema.ts:
    - Added lastShootTime (server-only, not replicated) for fire rate enforcement
  - Updated packages/client/src/App.ts:
    - Wired shootingSystem.setReloadCallback() to network.send('reload')
    - R key check in animate loop: consumeReload() triggers shootingSystem.startReload()
    - Fire condition now checks !shooting.getIsReloading()
    - syncAmmoFromServer(): reads local player's ammo/isReloading from Colyseus state, calls shootingSystem.syncAmmo()
    - HUDState now includes isReloading and reloadProgress fields
  - Updated packages/client/src/ui/GameHUD.ts:
    - HUDState interface: added isReloading?, reloadProgress?
    - Added .hud-reload element ("RELOADING..." text with pulse animation)
    - update() shows/hides reload indicator based on isReloading
  - Updated packages/client/index.html:
    - CSS for .hud-reload: orange text, pulsing animation (0.6s ease-in-out)

## Completed this session (continued 4)
- TASK-024 (Server hit detection: raycast, damage, kills)
  - Created packages/server/src/physics/hitDetection.ts:
    - rayAABB(): slab-method ray-AABB intersection, returns distance or null
    - raySphere(): analytic ray-sphere intersection for headshot detection
    - performHitDetection(): iterates all alive enemy players, tests head sphere (HEAD_RADIUS=0.15 at HEAD_OFFSET_Y=1.6) then body AABB (PLAYER_RADIUS × PLAYER_HEIGHT), returns closest hit with isHeadshot flag
    - Skips self and teammates (no friendly fire)
    - HitTarget interface: {sessionId, x, y, z}
    - HitResult interface: {targetId, isHeadshot, distance}
  - Updated packages/server/src/rooms/GameRoom.ts shoot handler:
    - Server-authoritative origin: player position + EYE_HEIGHT (ignores client origin)
    - Validates and normalizes client direction vector (rejects NaN, zero-length)
    - Builds targets list from alive players in room
    - Calls performHitDetection() with weapon range as max distance
    - On hit: damage = config.damage.head or config.damage.body (weapons.ts already has both)
    - victim.hp -= damage, sends 'hit' event to shooter (HitEvent with targetId, damage, isHeadshot, direction)
    - Sends 'damaged' event to victim (damage, isHeadshot, attackerId, reversed direction for damage indicator)
    - On kill (hp <= 0): victim.isAlive=false, killer.kills++, victim.deaths++
    - Creates KillEventSchema and pushes to state.killFeed (synced to all clients via Colyseus)
    - Broadcasts 'kill' event (KillEvent with killerName, victimName, weaponId, isHeadshot)
    - Console logs kills with weapon and headshot indicator

## Completed this session (continued 5)
- TASK-026 (Round system: server-side state machine)
  - Verified fully implemented in packages/server/src/systems/RoundSystem.ts:
    - State machine: lobby → weapon_select (5s) → countdown (3s via clock.setTimeout) → playing (120s) → round_end (3s) → loop or match_end
    - enterWeaponSelect(): sets status, increments currentRound, clears weapon selections, starts 5s timer
    - enterCountdown(): applies weapon selections, spawns players (spawnPlayers()), broadcasts 'countdown', schedules transition to playing via clock.setTimeout
    - tickPlaying(): decrements roundTimer, checks team elimination (checkTeamElimination()), timer expiry → draw
    - enterRoundEnd(): updates score for winning team, broadcasts 'roundEnd' (RoundEndEvent)
    - enterMatchEnd(): determines winner, broadcasts 'matchEnd' (MatchEndEvent), calls onMatchEnd callback
    - tickRoundEnd(): checks if roundsToWin reached → match_end, else → next weapon_select
    - selectWeapon(): stores weapon selection during weapon_select phase
    - spawnPlayers(): resets HP=100, isAlive=true, positions at team spawn points from map config
    - isPlayable()/isShootingAllowed(): guards input/shoot handlers during countdown/non-playing phases
    - returnToLobby(): resets all state for rematch
  - GameRoom integration (packages/server/src/rooms/GameRoom.ts):
    - RoundSystem instantiated in onCreate(), receives state, clock, broadcast callback
    - 'startGame' handler calls roundSystem.startGame()
    - 'selectWeapon' handler validates and forwards to roundSystem.selectWeapon()
    - 'returnToLobby' handler for admin after match_end
    - shoot/reload/input handlers gated by roundSystem.isShootingAllowed()/isPlayable()
    - onTick() calls roundSystem.tick()
  - Client integration (packages/client/src/App.ts):
    - checkGameStatus() handles match_end → MATCH_END screen, lobby → LOBBY (rematch)
    - showMatchEndScreen(): displays winner, score, K/D table, Rematch (admin) / Quit buttons
    - Network listeners for 'countdown', 'roundStart', 'roundEnd', 'matchEnd' events
    - syncLobbyState() auto-transitions to PLAYING when status becomes weapon_select/playing
  - All shared types exist: GameStatus includes all 5 states, RoundEndEvent, MatchEndEvent in network.ts
  - All constants: WEAPON_SELECT_TIME=5, ROUND_COUNTDOWN=3, ROUND_END_PAUSE=3, ROUND_TIME_LIMIT=120
  - TypeScript compiles cleanly for both server and client

## Completed this session (continued 6)
- TASK-027 (Weapon select screen — client)
  - Created packages/client/src/ui/WeaponSelectScreen.ts:
    - Dark overlay (rgba(10,10,25,0.85)) with z-index:20, shown during weapon_select phase
    - Title: "CHOOSE YOUR WEAPON" in orange
    - Countdown timer: large white text with orange glow, reads from state.roundTimer
    - 3 weapon cards with stats from shared/weapons.ts: DMG (body/head), RATE (rps), MAG, RELOAD, RANGE
    - AUTO tag on MP9 card (green badge)
    - Orange border highlight on selected card, hover lift effect
    - Default selection: Deagle (DEFAULT_WEAPON)
    - Click sends 'selectWeapon' message to server immediately
    - show()/hide() controlled by game status in checkGameStatus()
    - dispose() removes overlay from DOM
  - Updated packages/client/src/engine/ShootingSystem.ts:
    - Made weaponId, magazineSize, reloadTime mutable (removed readonly)
    - Added switchWeapon(newWeaponId): updates all weapon-dependent fields (ammo, magazine, reloadTime, raycaster.far), cancels reload, resets spread
  - Updated packages/client/src/App.ts:
    - WeaponSelectScreen created in startGameLoop(), disposed in stopGameLoop()
    - checkGameStatus() shows weapon select overlay when status === 'weapon_select', hides otherwise
    - Timer updated each state change from state.roundTimer
    - onSelect callback sends selectWeapon message to server
    - Added syncWeaponFromServer(): detects currentWeapon change on local PlayerSchema, calls shootingSystem.switchWeapon()
    - getLocalWeaponId() now reads from server state (was hardcoded 'deagle')
  - Updated packages/client/index.html:
    - Full CSS for .weapon-select-overlay, .ws-title, .ws-timer, .ws-cards, .ws-card, .ws-card-selected
    - Card layout: 200px width, dark panel (#16213e), orange border on selected, hover lift
    - Stat rows: label (grey) + value (light), flex justify-content: space-between

## Completed this session (continued 7)
- TASK-021 (Client-side prediction and server reconciliation)
  - Created packages/client/src/engine/ClientPrediction.ts:
    - Ring buffer of PendingInput entries (seq, MovementInput, deltaTime), max 256
    - reconcile(): discards confirmed inputs (seq <= lastProcessedSeq), snaps to server state, replays unconfirmed inputs through applyMovement() + CollisionWorld
    - Teleport threshold: >2 units = instant snap, else smooth correction via lerp offset
    - consumeCorrectionDelta(): returns per-frame smooth correction (20% of remaining offset per frame)
    - clear(): resets buffer and offset (called on respawn/round start)
  - Updated packages/client/src/engine/FPSController.ts:
    - Added getPhysicsState(): returns copy of internal PhysicsState
    - Added setPhysicsState(): overrides physics + position (for teleport reconciliation)
    - Added applyPositionDelta(): adds smooth correction offset to position
  - Updated packages/client/src/App.ts:
    - ClientPrediction created in startGameLoop() with same CollisionWorld as FPSController
    - sendInput() stores MovementInput in prediction buffer alongside server send
    - syncRemotePlayers() now calls reconcileLocalPlayer() for local player
    - reconcileLocalPlayer() reads lastProcessedSeq from server PlayerSchema, calls prediction.reconcile()
    - animate() applies consumeCorrectionDelta() each frame after movement update
    - Prediction buffer cleared on countdown (respawn) event
    - Cleaned up on stopGameLoop()

## Completed this session (continued 8)
- TASK-022 (Entity interpolation for remote players)
  - Created packages/client/src/engine/InterpolationBuffer.ts:
    - Stores timestamped snapshots (x, y, z, yaw, pitch) per remote player
    - Ring buffer of ~TICK_RATE+2 snapshots (~1 second history)
    - Render delay = 2/TICK_RATE seconds (100ms at 20Hz) — always have snapshot pair to interpolate
    - getInterpolated(): finds two snapshots bracketing renderTime, returns lerp'd transform
    - lerpAngle() handles wrap-around at ±PI for smooth yaw interpolation
    - Clamps to oldest/newest snapshot if renderTime is outside buffer range
    - clear() for round reset / respawn
  - Updated packages/client/src/engine/RemotePlayerManager.ts:
    - Each RemotePlayer now has an InterpolationBuffer
    - Replaced updatePlayer() with pushSnapshot(): stores server state in buffer
    - Added updateInterpolation(): called every frame, reads interpolated transform and applies to group
    - Added clearBuffers(): clears all interpolation buffers (round reset)
  - Updated packages/client/src/App.ts:
    - syncRemotePlayers() now calls pushSnapshot() instead of updatePlayer()
    - onPlayerAdd pushes initial snapshot with pitch
    - animate() calls remotePlayers.updateInterpolation() every frame
    - Countdown handler clears interpolation buffers alongside prediction buffer
    - PlayerData interface now includes pitch field

## Current state
- TASK-001 through TASK-022 + TASK-023 + TASK-024 + TASK-026 + TASK-027 all done
- Server: boots on 2567, room codes, nicknames, move/look with physics+collisions, game loop at 20Hz, updateSettings + startGame handlers, shoot handler with ammo/fire-rate validation + server-side hit detection + damage + kill tracking, reload handler with timer, RoundSystem state machine (weapon_select → countdown → playing → round_end → match_end), selectWeapon handler, returnToLobby handler
- Client: Main Menu → Lobby → Weapon Select → Playing → Match End flow, NetworkManager, App state machine, 3D warehouse scene with FPS controls, physics, weapon model, shooting system with visual effects + reload + weapon switching, remote player capsules synced via Colyseus, in-game HUD with reload indicator, weapon select screen with 3 cards + timer, match end screen with rematch/quit, round event listeners, client-side prediction with server reconciliation, **entity interpolation for smooth remote player movement**

## Next priority tasks (all deps met)
- TASK-028: SSG-08 model (deps: TASK-023 done) — high
- TASK-029: MP9 model (deps: TASK-023 done) — high
- TASK-031: Kill Feed UI (deps: TASK-018 done, TASK-024 done) — medium
- TASK-032: Scoreboard UI (deps: TASK-018 done) — medium
- TASK-033: Damage indicator/hitmarker (deps: TASK-018 done, TASK-024 done) — medium
- TASK-035: Match end screen (deps: TASK-026 done) — medium
- TASK-036: MapLoader (deps: TASK-026 done) — medium

## Notes
- Server input handling: per-message processing (not buffered), applies physics + collisions immediately
- Input validation: MAX_DELTA_TIME = 0.25s, rejects non-finite, negative, wrong types
- Server CollisionWorld in packages/server/src/physics/ — mirrors client version without Three.js
- lastProcessedSeq (uint32) in PlayerSchema — clients will use for prediction reconciliation
- velocityY, isGrounded, lastShootTime are server-only fields (not @type decorated, not replicated)
- @colyseus/schema v2.0.37 uses legacy decorators (experimentalDecorators: true)
- tsx needs --tsconfig flag to respect experimentalDecorators from tsconfig.json
- Room code uses ABCDEFGHJKLMNPQRSTUVWXYZ23456789 charset (no 0/O/1/I confusion)
- Room code is exposed as metadata for client discovery via getAvailableRooms()
- Nickname regex: /^[A-Za-z0-9_]+$/, length 3-16
- PlayerSchema uses flat float32 for position/rotation (not nested Schema) for bandwidth efficiency
- GameState.players is MapSchema<PlayerSchema> keyed by sessionId
- GameState.killFeed is ArraySchema<KillEventSchema>
- PLAYER_SPEED = 250 u/s, GRAVITY = -20, JUMP_VELOCITY = 8, EYE_HEIGHT = 1.65m
- PLAYER_RADIUS = 0.3, PLAYER_HEIGHT = 1.8 (collision capsule approximated as AABB)
- applyMovement() is a pure function — returns new state, doesn't mutate input. Ready for server use.
- CollisionWorld.resolve() runs up to 4 iterations to handle corner sliding
- Mouse sensitivity = 0.002, Euler order YXZ for proper FPS camera
- Colyseus 0.15.57 on server, colyseus.js ^0.15.0 on client
- Scene uses PCFSoftShadowMap, fog (30-60 range), directional light shadow camera covers 25 units
- Weapon overlay uses separate scene+camera with autoClear=false, clearDepth before render
- Deagle recoil: 120ms duration, sin-based kick curve, 0.04 up / 0.06 back / 0.15 rad rotation
- App class: setState('playing') starts game loop, setState('menu') stops it and disposes 3D
- App exposed as window.app for console testing: app.setState('playing'), app.setState('menu')
- Screen containers in #ui-root: menu-screen, lobby-screen, playing-screen, match-end-screen
- Canvas is display:none except when in PLAYING state
- RemotePlayerManager uses cylinder+sphere (body+head) per remote player, team-colored
- Input sending: every frame when pointer locked, InputMessage with seq/keys/yaw/pitch/deltaTime
- Remote player sync: onStateChange reads state.players MapSchema, updates capsule positions
- onPlayerAdd/onPlayerRemove handle dynamic spawn/despawn of capsule meshes
- LobbyScreen polls Colyseus state every 200ms via setInterval (not event-driven)
- LobbyScreen auto-transitions to PLAYING when server status !== 'lobby'
- updateSettings rebuilds CollisionWorld on map change
- startGame validates team staffing: 1 per team for 1v1, 2 per team for 2v2
- Admin reassignment on leave already handled in GameRoom.onLeave()
- ShootingSystem: raycaster.far set to weapon range, PointLight for muzzle flash, LineBasicMaterial for tracers
- ShootingSystem spread: base + moving (overrides base) + sustained * consecutiveShots; resets after 300ms idle
- Decals use ring buffer (MAX_DECALS=64) to prevent memory growth; CircleGeometry oriented via lookAt(point+normal)
- ShootMessage sent to server: {seq, timestamp, origin: Vec3, direction: Vec3}
- Server 'shoot' handler: validates alive, !isReloading, ammo>0, fire rate via lastShootTime, decrements ammo
- Server 'reload' handler: validates !isReloading, ammo<magazine, sets isReloading=true, clock.setTimeout completes reload
- Client reload: auto-reload at 0 ammo, R key for manual, blocks firing, syncs from server state
- Reload timer stored in seconds (reloadTime ms / 1000), ticks down each frame in update()
- HUD reload indicator: "RELOADING..." with CSS pulse animation, shown/hidden via isReloading flag
- GameHUD reads hp/score/timer from Colyseus state with fallback defaults
- GameHUD.update() called every frame in animate loop
- HUD container appended to playing-screen div, z-index: 10
- Crosshair is pure CSS (no canvas/WebGL), green #00ff44, 20px size
- WeaponId in HUD now reads from server state (currentWeapon on PlayerSchema)
- ShootingSystem.switchWeapon() handles weapon changes: updates ammo, magazine, reloadTime, raycaster range
- WeaponSelectScreen overlay: z-index:20 (above HUD z-index:10), shown during weapon_select phase only
- ClientPrediction: ring buffer of 256 max PendingInput entries, reconcile on server ack via lastProcessedSeq
- Reconciliation: discard confirmed inputs, snap to server pos, replay unconfirmed through applyMovement+collisions
- Teleport threshold: >2 units = instant snap; <2 units = smooth lerp at 20% per frame via correction offset
- Prediction buffer cleared on countdown event (respawn) and on stopGameLoop
- FPSController exposes getPhysicsState/setPhysicsState/applyPositionDelta for reconciliation
- Hit detection: server-authoritative origin (player.x/y/z + EYE_HEIGHT), client direction (validated+normalized)
- Ray-AABB slab method and ray-sphere for headshot detection in packages/server/src/physics/hitDetection.ts
- Head hitbox: sphere at (x, y+1.6, z) radius 0.15; Body hitbox: AABB from feet to PLAYER_HEIGHT
- Weapon damage from config.damage.head / config.damage.body (already separated in weapons.ts)
- Server sends 'hit' to shooter, 'damaged' to victim, 'kill' broadcast to all; killFeed via Colyseus state sync
- No friendly fire: hit detection skips players on same team
- Entity interpolation: InterpolationBuffer stores snapshots per remote player, renders at 100ms delay (2 ticks at 20Hz)
- InterpolationBuffer: ring buffer of ~22 snapshots, lerp position, lerpAngle for yaw (shortest path wrap-around)
- RemotePlayerManager.pushSnapshot() replaces old updatePlayer(), updateInterpolation() called every frame
- Interpolation buffers cleared on countdown event alongside prediction buffers
