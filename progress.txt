# BrowserStrike — Notes for next iteration

## Completed this session
- TASK-008 (First-person weapon model: Desert Eagle)
  - Created packages/client/src/engine/WeaponModel.ts:
    - Low-poly Desert Eagle built from BoxGeometry/CylinderGeometry primitives (slide, barrel, muzzle, frame, trigger guard, trigger, grip, magazine base, front/rear sights)
    - Rendered as overlay scene (separate Three.js scene + camera) so weapon never clips into world geometry
    - Recoil animation: sin-based kick (up + back + pitch rotation) over 120ms, smooth recovery
    - Semi-auto fire: single click = single shot, no hold-to-fire (mouseDown edge detection)
    - Fire rate limiting: 500ms between shots (matches WEAPONS.deagle.fireRate from shared constants)
    - Weapon positioned in classic FPS lower-right corner (0.25, -0.22, -0.45)
  - Updated InputManager.ts: added mouseDown tracking (mousedown/mouseup on LMB)
  - Updated SceneManager.ts: added renderOverlay() for weapon scene (clears depth, renders on top)
  - Updated main.ts: creates WeaponModel, wires fire input, renders weapon overlay each frame, handles resize for weapon camera aspect

## Current state
- TASK-001 through TASK-008 all done
- Server boots on port 2567, responds to HTTP, logs "GameRoom registered"
- Client builds and type-checks; 3D warehouse scene with full FPS controls, gravity, jump, AABB collisions
- Desert Eagle model visible in lower-right, LMB fires with recoil animation, semi-auto enforced, fire rate limited

## Next priority tasks (all deps met)
- TASK-012: Colyseus State Schema (deps: TASK-004 ✓, TASK-002 ✓) — critical, blocks entire multiplayer chain
- TASK-009: Shooting system / raycasting (deps: TASK-008 ✓) — high, muzzle flash + bullet tracer + impact marks

## Notes
- PLAYER_SPEED = 250 u/s, GRAVITY = -20, JUMP_VELOCITY = 8, EYE_HEIGHT = 1.65m
- PLAYER_RADIUS = 0.3, PLAYER_HEIGHT = 1.8 (collision capsule approximated as AABB)
- applyMovement() is a pure function — returns new state, doesn't mutate input. Ready for server use.
- CollisionWorld.resolve() runs up to 4 iterations to handle corner sliding
- Mouse sensitivity = 0.002, Euler order YXZ for proper FPS camera
- Colyseus 0.15.57 on server, colyseus.js ^0.15.0 on client
- Scene uses PCFSoftShadowMap, fog (30-60 range), directional light shadow camera covers 25 units
- Weapon overlay uses separate scene+camera with autoClear=false, clearDepth before render
- Deagle recoil: 120ms duration, sin-based kick curve, 0.04 up / 0.06 back / 0.15 rad rotation
